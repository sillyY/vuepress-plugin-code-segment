# Setting
Only support `showText`、`hideText`, `minHeight` now

```
plugins: [
    ['code-segment', {
        showText: '展示代码',
        hideText: '隐藏代码',
        minHeight: 40
    }]
]
```
> Tip: All properties have default values, the above display is the default value.