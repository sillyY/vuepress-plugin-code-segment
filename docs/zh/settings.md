# 配置项

目前仅支持`showText`、`hideText`, `minHeight`

```
plugins: [
    ['code-segment', {
        showText: '展示代码',
        hideText: '隐藏代码',
        minHeight: 40
    }]
]
```
> Tip: 所有属性均存在默认值, 上述展示为默认值.